enrich int-div with

functions 

          . ^2 : int → int;
partial functions
          sqrt : int → int with λ i. 0 ≤ i;

axioms 

Square-def : i ^2 = i * i; used for : ls;

Sqrt-def : 0 ≤ i → sqrt(i) ^2 ≤ i ∧ i < (sqrt(i) +1) ^2; used for : nokodkodax;

end enrich
