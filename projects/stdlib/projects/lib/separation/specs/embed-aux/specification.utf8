enrich heap-fun, heap-aux with

functions
.[.] : aux × (heap → bool) → heap-aux → bool;

;; variables
;; G : (ref → aux) → bool;

axioms

lift-aux:
a[P](mk(rs,f,g))
  ↔ P(mk(rs,f)) ∧ ∀ r. r ∈ rs → g(r) = a;

end enrich
